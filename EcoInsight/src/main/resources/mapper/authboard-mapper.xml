<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.semi.ecoinsight.authboard.model.dao.AuthBoardMapper">
	<insert id="insertAuthBoard">
		INSERT
		INTO
			TB_AUTH_BOARD
			(
				BOARD_NO,
				MEMBER_NO,
				CATEGORY_ID,
				BOARD_TITLE,
				BOARD_CONTENT,
				CREATED_DATE,
				MODIFIED_DATE,
				VIEW_COUNT,
				IS_DELETED
			)
		VALUES
			(
				#{boardNo},
				#{memberNo},
				#{categoryId},
				#{boardTitle},
				#{boardContent},
				SYSDATE,
				SYSDATE,
				0,
				'N'
			)
	</insert>
	
	<select id="selectAuthBoardList"
			parameterType="long"
			resultType="com.semi.ecoinsight.authboard.model.dto.AuthBoardDTO">
	</select>
	
	<update id="updateAuthBoard" parameterType="com.semi.ecoinsight.authboard.model.vo.AuthBoard">
		UPDATE
			TB_AUTH_BOARD
		SET
			BOARD_TITLE = #{boardTitle},
			BOARD_CONTENT = #{boardContent}
		WHERE
			BOARD_NO = #{boardNo}
	</update>
	
	<delete id="deleteAuthBoard">
		DELETE
		FROM
			TB_AUTH_BOARD
		WHERE
			BOARD_NO = #{boardNo}
	</delete>
</mapper>